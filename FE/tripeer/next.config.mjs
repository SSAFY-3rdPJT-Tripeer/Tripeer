/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [],
  },

  eslint: {
    ignoreDuringBuilds: true,
  },
  // distDir : "build"
  // output: 'export',
};

export default nextConfig;
